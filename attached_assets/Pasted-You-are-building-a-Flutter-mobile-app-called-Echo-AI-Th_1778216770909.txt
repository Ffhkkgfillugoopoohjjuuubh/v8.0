You are building a Flutter mobile app called "Echo AI". This is a fresh start.
Delete ALL existing files. Then create a complete Flutter project with the
exact features below.

══════════════════════════════════════════════════
1. PROJECT TYPE & DEPENDENCIES
══════════════════════════════════════════════════
- Create a standard Flutter project for Android.
- Use these packages in pubspec.yaml:
  http, flutter_tts, google_mlkit_text_recognition, image_picker,
  path_provider, shared_preferences, google_fonts, flutter_markdown,
  permission_handler, uuid, intl, flutter_localizations

══════════════════════════════════════════════════
2. API CONFIGURATION (hardcode everything)
══════════════════════════════════════════════════
- Groq API Key: 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
- Groq endpoint: https://api.groq.com/openai/v1/chat/completions
- AI Model: openai/gpt-oss-120b

══════════════════════════════════════════════════
3. MATHEMATICAL PRE‑PROCESSING LAYER (MUST BE BUILT)
══════════════════════════════════════════════════
Before ANY text is sent to the Groq API, it MUST pass through a
MathPreprocessor class. This class must be called for both the user's typed
question and the OCR‑extracted page text.

The MathPreprocessor must perform these exact transformations:
- Convert all LaTeX fraction commands: \frac{a}{b} → (a)/(b)
- Convert all LaTeX sqrt commands: \sqrt{x} → sqrt(x), \sqrt[y]{x} → y_root(x)
- Convert Unicode radical symbol √ → sqrt
- Remove ALL LaTeX wrappers: \(, \), \[, \], $$, $
- Remove all stray backslashes: \
- Convert Unicode fraction characters: ½→1/2, ⅓→1/3, ⅔→2/3, ¼→1/4, ¾→3/4
- Collapse multiple spaces to a single space
- Replace common Indian fraction notations like "½" etc.
- Ensure decimals always use a point (2.5) not a comma (2,5)
- If the user writes something like "x−1/x=3", ensure it becomes
  "x - 1/x = 3" with proper spacing so the AI reads it correctly.

This preprocessor must be applied to EVERY user message and to the page
context BEFORE calling the Groq API.

══════════════════════════════════════════════════
4. SYSTEM PROMPT THAT PREVENTS PARSING ERRORS
══════════════════════════════════════════════════
When calling the Groq API, use this exact system prompt.
Replace {language} and {style} with the actual user selections.
You are Echo AI, a brilliant, patient teacher.
You MUST write your entire answer in {language}.
{style}

BEFORE YOU SOLVE ANY PROBLEM, YOU MUST DO THIS:

Rewrite the given problem in a clean, unambiguous plain‑text format.
Write all fractions as (numerator)/(denominator) with parentheses.
Write all square roots as sqrt(...).
Write all powers as x^2, x^3, etc.
Verify that this rewritten problem matches the original problem.
If you are unsure, ask the user to confirm.

WHEN YOU SOLVE:

Give the direct answer or explanation.

Break the concept into clear, numbered steps.

Show every calculation step on its own line.

Never use LaTeX or special symbols. Write everything in plain text.

For radical equations: square both sides carefully, check for
extraneous solutions.

For fraction equations: multiply both sides by the LCD first.

For decimals: always use a point (2.5), never a comma.

After solving, verify your answer by plugging it back into the
original problem. If it does not satisfy the original equation,
re‑solve.

FORMAT:

Use bold for important words.

Use bullet points for lists.

Keep paragraphs short and readable.

End with a short summary and one encouraging question.

text

Where:
- {language} is the language selected in the dropdown (e.g. "Bengali", "Hindi").
- {style} is:
  * If Casual: "Use casual, everyday spoken words. Keep ALL scientific and
    technical terms in English."
  * If Formal: "Use formal, academic language. Translate all terms, including
    scientific ones, into this language."

- The user message sent to Groq is:
  "Page content: [preprocessed extracted text]\n\nUser question: [preprocessed user prompt]"

══════════════════════════════════════════════════
5. SCREENS & NAVIGATION
══════════════════════════════════════════════════
Bottom navigation with 3 tabs:
- Scanner (home)
- Notebook
- Settings

══════════════════════════════════════════════════
6. SCANNER SCREEN (main screen)
══════════════════════════════════════════════════
- User taps "Add Images" and picks up to 10 photos (camera or gallery).
- After EVERY photo is selected, ML Kit (google_mlkit_text_recognition)
  automatically extracts the text. There is NO "Extract Text" button.
- While extracting, show a small progress indicator and the message
  "Reading text from image..."
- The extracted text is stored in `currentPageContext` and also shown in
  a collapsible, read‑only text area so the user can verify it.
- The extracted text is AUTOMATICALLY passed through the MathPreprocessor
  before being stored.
- Below the extracted text, there is a chat‑style input field where
  the user can type a question (e.g. "Explain this in simple Bengali").
- The user selects the language from a DROPDOWN called
  "AI Response & Voice Language" (this controls BOTH the language the AI
  writes in AND the language used for Read Aloud). Default: English.
- Beside the dropdown, there is a toggle switch labelled "Casual / Formal":
  - Casual (default): scientific/technical terms stay in English.
  - Formal: everything is fully translated, including scientific terms.
- The user taps SEND. The typed prompt is preprocessed by the
  MathPreprocessor, then combined with the preprocessed page context,
  and sent to the Groq API.
- The AI response is displayed in a chat bubble using flutter_markdown.
- Below EVERY AI response there are three small buttons:
  "Copy", "Save to Notebook", and "Read Aloud" (speaker icon).
- When "Read Aloud" is tapped, flutter_tts speaks the AI response text.
  BEFORE speaking, the text is cleaned: all markdown symbols
  (** , * , # , ` , ```, -, >, [, ]) are removed, but line breaks
  are preserved.
  The spoken language matches the selected "AI Response & Voice Language".
  A small status chip with Stop / Pause / Resume appears while TTS
  is active.

══════════════════════════════════════════════════
7. NOTEBOOK SCREEN
══════════════════════════════════════════════════
- Shows a list of saved notes (title, subject, date).
- Tap a note to open it in a rich‑text editor.
- Inside the editor, an "AI Assistant" button allows the user to modify
  the note using the same Groq API.
- Notes are stored locally as JSON files.

══════════════════════════════════════════════════
8. SETTINGS SCREEN
══════════════════════════════════════════════════
- Font Size slider (14–30)
- TTS Volume, Pitch, Speech Rate sliders
- Dark Mode / Light Mode toggle
- Clear All Notes button
- App Version info
- There is NO separate "Voice Language" setting anywhere.

══════════════════════════════════════════════════
9. QUIZ GENERATOR (on Scanner screen)
══════════════════════════════════════════════════
- A "Generate Quiz" button that uses the current page context.
- User selects number of questions (1–15) and time limit (5–60 min).
- Quiz screen shows one question at a time with a countdown timer.
- At the end, AI grades the quiz and shows correct answers with
  explanations.

══════════════════════════════════════════════════
10. TTS (READ ALOUD) – MUST WORK
══════════════════════════════════════════════════
- Use flutter_tts. Before speaking, clean the text by removing all
  markdown formatting characters (**, *, #, `, ```, -, >, [, ], etc.).
- The language code for flutter_tts must be set from the selected
  "AI Response & Voice Language" using the standard BCP-47 codes
  (e.g. en-US, bn-IN, hi-IN, ta-IN, …).
- Include Stop, Pause, Resume controls.
- The TTS pitch, rate, and volume must reflect the Settings sliders.

══════════════════════════════════════════════════
11. AFTER THE CODE IS GENERATED
══════════════════════════════════════════════════
- Tell me: "All files created. You can now download the project as a ZIP."
- Then I will download, push to GitHub, and build the APK.