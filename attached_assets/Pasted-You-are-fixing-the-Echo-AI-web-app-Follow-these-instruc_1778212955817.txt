You are fixing the Echo AI web app. Follow these instructions in order.
Do NOT change anything else unless I say so.

══════════════════════════════════════════════════
1. FIX THE OCR (ABSOLUTELY NO EXTERNAL API)
══════════════════════════════════════════════════
- Remove ANY call to Groq Vision or Groq image API.
- OCR MUST use only Tesseract.js running inside the browser.
- When I select a photo, the image MUST appear in the UI immediately.
- There must be an “Extract Text” button (do NOT auto-run OCR).
- When I click “Extract Text”, a progress message “Extracting text… please wait” must appear.
- If Tesseract fails, show an error message like “Could not read text from this image.”
- The extracted text must be stored as `currentPageContext` and shown in a small read‑only text area so I can verify it.

══════════════════════════════════════════════════
2. SIMPLIFY SETTINGS
══════════════════════════════════════════════════
- Remove the “Voice Language” dropdown entirely.
- Keep a SINGLE dropdown called “AI Response & Voice Language”.
  This dropdown controls BOTH the language the AI writes in AND the language used for Read Aloud.
- Default value: English.
- Beside this dropdown, add a toggle switch called “Casual / Formal”.
  - When “Casual” (default): the AI should use everyday spoken language.
    Scientific/technical terms MUST stay in English (e.g. “acceleration” written in English letters inside a Bengali sentence).
  - When “Formal”: the AI must fully translate everything, including scientific terms, into the selected language (e.g. “ত্বরণ”).

══════════════════════════════════════════════════
3. THE GROQ SYSTEM PROMPT
══════════════════════════════════════════════════
When the user sends a message, use the Groq API with this exact system prompt.
Replace {language} and {style} with the actual values from the dropdown and toggle.
You are Echo AI, a brilliant, patient teacher.
You MUST write your entire answer in {language}.
{style}
Your answer must be beautifully structured, like this:

First, give the direct answer or explanation.

Then, break the concept into clear, numbered steps.

End with a short summary and one encouraging question.
Use bold for important words.
Use bullet points for lists.
Keep paragraphs short and readable.
Never use LaTeX or special symbols.
Write fractions as (a)/(b).
Write square roots as sqrt(x).
If the user asked a question from a textbook page you have seen, use the page context to answer.

text

Where {style} is:
- If Casual: "Use casual, everyday spoken words. Keep ALL scientific and technical terms in English."
- If Formal: "Use formal, academic language. Translate all terms, including scientific ones, into this language."

4. DO NOT auto‑submit. The user MUST type a prompt and click “Send” before anything is sent to Groq.

══════════════════════════════════════════════════
5. READ ALOUD FIX
══════════════════════════════════════════════════
- Under every AI response, add a “Read Aloud” button (speaker icon).
- When clicked, use the browser’s SpeechSynthesis API.
- BEFORE speaking, clean the text by removing all markdown characters: **, *, #, `, ```, -, >, and any other formatting symbols.
- Set the spoken language to the language selected in “AI Response & Voice Language”.
- While speaking, show a small status bar with “Stop / Pause / Resume” buttons.
- The voice must be natural and clear. Do NOT speak asterisks or hashes.

══════════════════════════════════════════════════
6. CHAT‑STYLE UI
══════════════════════════════════════════════════
- The extracted page text and the AI answers must be displayed in a clean, modern chat‑like interface.
- Use a light/dark‑aware design that looks professional, like ChatGPT.
- On mobile, the text must be large enough to read comfortably.

══════════════════════════════════════════════════
7. AFTER ALL CHANGES
══════════════════════════════════════════════════
- Restart the app so I can test.
- Show me the new live URL.